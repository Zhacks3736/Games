<html>
<head>
    <base target="_blank">
    <style>
        /* Ensures any G+ text placeholders in the wrapper are hidden */
        #unblocked-text { display: none !important; }

        /* Fullscreen button styling */
        .fullscreen-button {
            padding: 10px 15px;
            background: #000;
            color: #fff;
            border: 2px solid #4a148c;
            border-radius: 5px;
            font: bold 14px Arial;
            cursor: pointer;
            position: fixed;
            bottom: 20px;
            right: 20px;
            z-index: 1002;
        }
        .fullscreen-button:hover { background: #4a148c; }
        body, html { margin: 0; padding: 0; overflow: hidden; height: 100%; }
        #fr { display: none; }
    </style>
</head>
<body>
    <div id="container">
        <iframe id="fr" width="100%" height="100%" frameborder="0" allowfullscreen=""></iframe>
        <p id="unblocked-text"></p>
        <button class="fullscreen-button" type="button" onclick="openFullscreen()">FULLSCREEN</button>
    </div>

    <script>
        // 🔑 YOUR RAW GITHUB LINK
        const FILE_URL = 'https://raw.githubusercontent.com/Zhacks3736/Games/main/1.xml'; 

        // Function to load the game automatically
        function loadGame() {
            const iframe = document.getElementById("fr");
            
            fetch(FILE_URL)
                .then(response => {
                    if (!response.ok) {
                        throw new Error(`HTTP error! status: ${response.status}`);
                    }
                    return response.text();
                })
                .then(text => {
                    iframe.contentDocument.open();
                    iframe.contentDocument.write(text);
                    iframe.contentDocument.close();
                    
                    iframe.style.display = "block";
                })
                .catch(err => {
                    console.error('Error loading game content:', err);
                    alert('Error: Could not load the game module.');
                });
        }

        // Function for the fullscreen button
        function openFullscreen() {
            fetch(FILE_URL)
                .then(response => response.text())
                .then(text => {
                    const newWindow = window.open("", "_blank");
                    if (newWindow) {
                        newWindow.document.open();
                        newWindow.document.write(text);
                        newWindow.document.close();
                    } else {
                        alert('Full screen failed: Pop-up blocked or failed to open.');
                    }
                })
                .catch(err => {
                    console.error('Error opening fullscreen:', err);
                });
        }

        // Automatically call loadGame() when the page is fully loaded
        window.addEventListener('load', loadGame);
    </script>
</body>
</html>
